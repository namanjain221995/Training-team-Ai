name: "EC2 Deploy (Docker Compose: Orchestrator -> Pipeline)"

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: ec2-deploy-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Ensure main EC2 is running
        shell: bash
        run: |
          set -euo pipefail
          INSTANCE_ID="${{ secrets.EC2_INSTANCE_ID }}"

          STATE="$(aws ec2 describe-instances --instance-ids "$INSTANCE_ID" \
            --query 'Reservations[0].Instances[0].State.Name' --output text)"

          echo "Current state: $STATE"

          if [ "$STATE" = "stopping" ]; then
            echo "Instance is stopping. Waiting for stopped..."
            aws ec2 wait instance-stopped --instance-ids "$INSTANCE_ID"
            STATE="stopped"
          fi

          if [ "$STATE" = "pending" ]; then
            echo "Instance is pending. Waiting for running..."
            aws ec2 wait instance-running --instance-ids "$INSTANCE_ID"
          elif [ "$STATE" = "running" ]; then
            echo "Instance already running."
          elif [ "$STATE" = "stopped" ]; then
            echo "Starting instance..."
            aws ec2 start-instances --instance-ids "$INSTANCE_ID" >/dev/null
            aws ec2 wait instance-running --instance-ids "$INSTANCE_ID"
          else
            echo "ERROR: Unexpected instance state: $STATE"
            exit 1
          fi

      - name: Wait for EC2 status checks OK
        shell: bash
        run: |
          set -euo pipefail
          aws ec2 wait instance-status-ok --instance-ids "${{ secrets.EC2_INSTANCE_ID }}"
          echo "Instance status checks OK."

      - name: Get EC2 public IP
        id: ec2ip
        shell: bash
        run: |
          set -euo pipefail
          INSTANCE_ID="${{ secrets.EC2_INSTANCE_ID }}"
          IP="$(aws ec2 describe-instances --instance-ids "$INSTANCE_ID" \
            --query 'Reservations[0].Instances[0].PublicIpAddress' --output text)"

          if [ "$IP" = "None" ] || [ -z "$IP" ]; then
            echo "ERROR: No public IP on instance."
            exit 1
          fi

          echo "public_ip=$IP" >> "$GITHUB_OUTPUT"
          echo "EC2 Public IP: $IP"

      - name: Wait for SSH
        shell: bash
        run: |
          set -euo pipefail
          HOST="${{ steps.ec2ip.outputs.public_ip }}"
          echo "Waiting for SSH on $HOST:22 ..."
          for i in {1..120}; do
            if nc -z -w 5 "$HOST" 22; then
              echo "SSH reachable."
              exit 0
            fi
            sleep 5
          done
          echo "ERROR: SSH not reachable."
          exit 1

      - name: Deploy + run docker compose on main EC2 (detached)
        uses: appleboy/ssh-action@v1.0.3
        env:
          EC2_PATH: ${{ secrets.EC2_PATH }}
        with:
          host: ${{ steps.ec2ip.outputs.public_ip }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script_stop: true
          command_timeout: 60m
          envs: EC2_PATH
          script: |
            set -euo pipefail
            cd "$EC2_PATH"

            echo "== Update code =="
            git fetch --all
            git reset --hard origin/main
            git clean -fd || true

            echo "== Ensure docker running =="
            sudo systemctl enable docker || true
            sudo systemctl start docker || true

            echo "== Choose compose command =="
            if command -v docker-compose >/dev/null 2>&1; then
              COMPOSE="docker-compose"
            else
              COMPOSE="docker compose"
            fi

            echo "== Compose down (clean) =="
            $COMPOSE down || true

            echo "== Compose up (build, detached) =="
            $COMPOSE up -d --build

            echo "== Show running containers =="
            $COMPOSE ps || true

            echo "== Follow orchestrator logs for a while (optional) =="
            # keep logs short so GitHub action doesn't run forever
            timeout 600 docker logs -f orchestrator || true

            echo "== Done. Orchestrator continues on EC2. =="